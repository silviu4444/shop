import { CartButtonEffectDirective } from './cart-button-effect.directive';

describe('CartButtonEffectDirective', () => {
  it('should create an instance', () => {
    const directive = new CartButtonEffectDirective();
    expect(directive).toBeTruthy();
  });
});
