import { ButtonPulseAnimationDirective } from './button-pulse-animation.directive';

describe('ButtonPulseAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonPulseAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
